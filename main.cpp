#include "token.hpp"
#include "lexer.hpp"
#include <fstream>
#include <iostream>
int main(){
  std::string text;
  std::ifstream file("test.v");
  while(getline(file, text)){
    
  }
  file.close();
}
